import { withHost } from '../utils/fetchHelper'

const CHECK_AUTHENTICATION_REQUEST = 'CHECK_AUTHENTICATION_REQUEST'
const CHECK_AUTHENTICATION_SUCCESS = 'CHECK_AUTHENTICATION_SUCCESS'
const CHECK_AUTHENTICATION_FAILURE = 'CHECK_AUTHENTICATION_FAILURE'

const checkAuthentication = () => {
  return (dispatch) => {
    dispatch()
  }
}